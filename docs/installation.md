# Installation

## Requirements
- Python 3.11+
- pip

## Install dependencies

pip install -r requirements.txt

## Run the application

streamlit run app.py

A `data/` folder will be created automatically.  
This is where your workspaces and projects are stored.
